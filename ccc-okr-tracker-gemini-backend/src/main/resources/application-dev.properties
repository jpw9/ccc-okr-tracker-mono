# Development Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/okr_tracker_db
spring.datasource.username=postgres
spring.datasource.password=Pa55w0rd
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Liquibase Configuration - enabled to run migrations
spring.liquibase.enabled=true

# Server Configuration - no context path for local dev
server.servlet.context-path=

# NEW: Keycloak/OAuth2 Resource Server Configuration (Dev)
# Issuer URI points to your Keycloak realm's /.well-known/openid-configuration endpoint
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://auth.ccc.net/auth/realms/Apps
#spring.security.oauth2.resourceserver.jwt.issuer-uri=https://keycloakrnd.ccc.net/auth/realms/Apps
# A common practice is to restrict the audience
#spring.security.oauth2.resourceserver.jwt.audiences=backend
