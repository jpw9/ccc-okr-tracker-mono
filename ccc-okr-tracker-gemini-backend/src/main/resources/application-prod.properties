# Production Database Configuration (PLACEHOLDERS)
spring.datasource.url=jdbc:postgresql://postgres.ccc.net:5432/ccc_okr_db?reWriteBatchedInserts=true
spring.datasource.username=okruser
spring.datasource.password=MOElnaNc2aO1Bgc
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# It is recommended to use ddl-auto=none for production in a real application.

# Liquibase Configuration for Production
# Liquibase is ENABLED in production to manage schema migrations
spring.liquibase.enabled=true
# Run migrations on startup
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml

spring.security.oauth2.resourceserver.jwt.issuer-uri=https://auth.ccc.net/auth/realms/Apps
#spring.security.oauth2.resourceserver.jwt.audiences=okr-tracker-backend

# CORS Configuration for Production (Tomcat 10 - port 8090)
app.cors.origins=http://10.1.155.28:8090/cccokrtracker,http://10.1.155.28:8090,http://10.1.155.28,https://okr-tracker.ccc.com

# Server Configuration
server.servlet.context-path=/okr-tracker-backend